{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser",
        "chrome",
        "--headed",
        "--viewport-size",
        "1440,900"
      ]
    },
    "cursor-ide-browser": {
      "comment": "Use Cursor built-in browser MCP for exploratory QA when Playwright MCP is unavailable"
    }
  }
}
